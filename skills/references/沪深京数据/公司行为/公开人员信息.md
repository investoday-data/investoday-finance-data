# 沪深京数据 / 公司行为 / 公开人员信息

---

## 高管持股变动明细

接口路径：`stock/exec-shrhld-chg`
请求方式：**`POST`**
tool_id：`list_stock_executive_shareholding_change`

接口说明：通过股票代码和日期范围查询公司高管及相关人员持股变动明细，返回包括变动日期、股票代码与名称、高管姓名与职务、持股人姓名与关系、变动前后持股数、成交均价及变动原因等核心信息，用于分析内部人交易行为和市场信号。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/exec-shrhld-chg --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 公司管理层名录

接口路径：`stock/mgmt-dirs`
请求方式：**`POST`**
tool_id：`list_stock_management_dirs`

接口说明：通过股票代码、任职状态、任职开始和结束日期等条件，查询沪深京上市公司管理层（如董事会、监事会、经营层等）的历史及现任人员名录，返回包括股票代码、股票名称、管理人员姓名、管理层类型、职务、任职状态、任职开始日期和离职日期等核心信息，用于公司治理结构分析、高管履历查询等场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `positionStatus` | — | string | 任职状态 | `1` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, positionStatus, beginDate, endDate, pageNum, pageSize
investoday-api stock/mgmt-dirs --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"positionStatus":"1"}'
```

---

## 董事长与总经理变更

接口路径：`stock/core-mgmt-chg`
请求方式：**`POST`**
tool_id：`list_stock_core_mgmt_changes`

接口说明：通过股票代码和任职起始日期范围查询上市公司核心管理层（如董事长、总经理）的历史变更记录，返回包括人员姓名、职位、变动原因、任职起止日期等详细信息，便于分析公司治理结构变化。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `positionStatus` | — | string | 任职状态 | `1` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, positionStatus, beginDate, endDate, pageNum, pageSize
investoday-api stock/core-mgmt-chg --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"positionStatus":"1"}'
```

---
