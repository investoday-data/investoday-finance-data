# 沪深京数据 / 公司行为 / 交易信息

---

## A股大宗交易明细

接口路径：`stock/block-trades`
请求方式：**`POST`**
tool_id：`list_stock_block_trades`

接口说明：通过股票代码和日期范围查询沪深京股票的大宗交易明细数据，返回包括交易日期、股票代码与名称、成交价、成交金额、成交量、买卖方营业部等核心信息，支持分页查询，适用于分析特定股票的大宗交易活动。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/block-trades --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 融资融券交易明细

接口路径：`stock/margin-trades`
请求方式：**`POST`**
tool_id：`list_stock_margin_trade`

接口说明：通过股票代码和日期范围（格式为yyyy-MM-dd HH:mm:ss）查询沪深京股票的融资融券交易明细，包括融资余额、融资买入额、融资偿还额、融券余量、融券卖出量、融券余量金额、融资融券余额等核心数据，用于分析市场杠杆资金动向和投资者情绪。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期 | `2025-01-01` |
| `endDate` | — | string | 结束日期 | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/margin-trades --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2025-01-01"}'
```

---

## 融资融券市场每日度统计

接口路径：`stock/mgn-trd-totals`
请求方式：**`POST`**
tool_id：`list_stock_margin_trade_totals`

接口说明：获取融资融券交易总量数据，包括融资买入额、融资余额、融券卖出量、融券余量、融券余额及融资融券余额等核心指标，适用于分析市场杠杆资金流动情况

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: beginDate, endDate, pageNum, pageSize
investoday-api stock/mgn-trd-totals --method POST --body-json '{"beginDate":"2020-01-01","endDate":"2025-01-01","pageNum":1}'
```

---

## 融资融券标的证券日保证金比例

接口路径：`stock/margin-requirement-ratios`
请求方式：**`POST`**
tool_id：`get_stock_margin_requirement`

接口说明：通过股票代码、开始日期和结束日期查询融资融券可充抵保证金证券的折算率及相关信息，包括股票代码、名称、折算率、发布机构、变更日期和下次变更日期等，支持分页查询。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/margin-requirement-ratios --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 融资融券标的详情

接口路径：`stock/margin-securities`
请求方式：**`POST`**
tool_id：`get_stock_margin_securities`

接口说明：通过股票代码查询融资融券标的证券及担保证券信息，包括证券代码、名称、标的证券属性、启用日期和停用日期，适用于融资融券业务中的证券资格查询和风险管理。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, pageNum, pageSize
investoday-api stock/margin-securities --method POST --body-json '{"stockCode":"000001","stockCodes":["000001","600519"],"pageNum":1}'
```

---
