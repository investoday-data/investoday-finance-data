# 沪深京数据 / 特色数据 / 公司分析

---

## 股票投资风险

接口路径：`stock/investment-risks`
请求方式：**`POST`**
tool_id：`get_stk_investment_risks`

接口说明：通过股票代码查询指定沪深京股票的投研风险分析数据，包含宏观风险、竞争风险、经营风险、项目风险等多维度风险描述及投资风险结论，同时提供研报细分栏目分类代码和引用ID，便于大模型进行深度的投资风险评估和决策支持。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, pageNum, pageSize
investoday-api stock/investment-risks --method POST --body-json '{"stockCode":"000001","stockCodes":["000001","600519"],"pageNum":1}'
```

---

## 股票基本面分析

接口路径：`stock/fundamentals`
请求方式：**`POST`**
tool_id：`get_stk_fundamentals`

接口说明：通过股票代码获取指定沪深京股票的基本面分析数据，包括股票名称、研报细分分类、基本面结论、业绩增长、盈利能力、现金流质量、新业务驱动、审批监管影响、财务健康度与杠杆等核心分析维度，以及相关研报引用ID和源表最新更新时间，适用于对个股进行深度财务分析和投资价值评估。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, pageNum, pageSize
investoday-api stock/fundamentals --method POST --body-json '{"stockCode":"000001","stockCodes":["000001","600519"],"pageNum":1}'
```

---

## 股票发展前景

接口路径：`stock/prospects`
请求方式：**`POST`**
tool_id：`get_stock_prospects`

接口说明：通过股票代码查询指定沪深京股票的发展前景分析，返回包括行业趋势、政策驱动、战略规划、项目进展、增长空间、市场渗透率、价格供需关系及发展前景结论等多维度深度分析报告，便于大模型进行股票基本面研究和投资价值评估。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, pageNum, pageSize
investoday-api stock/prospects --method POST --body-json '{"stockCode":"000001","stockCodes":["000001","600519"],"pageNum":1}'
```

---

## 股票主营业务与投资主题

接口路径：`stock/business-investment-themes`
请求方式：**`POST`**
tool_id：`get_stock_business_themes`

接口说明：通过股票代码查询指定沪深京股票的主营业务与投资主题分析报告，返回包括股票名称、业务地位、经营数据、发展前景、投资主题、技术路径与研发管线、品牌与渠道、产业链位置与产能情况以及主营业务结论等核心分析维度，用于深度了解公司的业务基本面和投资价值。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, pageNum, pageSize
investoday-api stock/business-investment-themes --method POST --body-json '{"stockCode":"000001","stockCodes":["000001","600519"],"pageNum":1}'
```

---
