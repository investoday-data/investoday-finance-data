# 沪深京数据 / 财务数据 / 财务当期指标数据

---

## 财务指标（成长能力）

接口路径：`stock/financial-indicators-growth`
请求方式：`GET`
tool_id：`get_stock_fin_ind_growth`

接口说明：通过股票代码和报告期截止日期范围（格式为yyyy-MM-dd），查询沪深京股票的财务成长能力指标，包括营业收入、净利润、每股收益、总资产、经营性净利润、营业利润、经营活动现金流量净额、净资产等关键财务指标的一年平均增长率以及营业收入和净利润的同比增长率，用于评估公司的成长潜力和财务健康状况。

### 输入参数

**Query 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Query 可选参数: beginDate, endDate, pageNum, pageSize
investoday-api stock/financial-indicators-growth stockCode=002594
```

---

## 财务衍生指标

接口路径：`stock/fin-der-inds`
请求方式：**`POST`**
tool_id：`list_stock_fin_derivative_inds`

接口说明：支持通过单个股票代码或多个股票代码数组，结合报告发布日期范围（格式yyyy-MM-dd）及分页参数，查询A股、B股相关股票的财务衍生指标数据，包含股票基本信息、报告时间信息，以及盈利能力、成长能力、偿债能力、营运能力、现金流、估值相关的多维度财务指标，可用于股票基本面分析、财务状况评估等投研场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/fin-der-inds --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 财务指标（每股指标）

接口路径：`stock/per-share-indicators`
请求方式：**`POST`**
tool_id：`list_per_share_indicators`

接口说明：通过股票代码和日期范围查询指定股票的每股财务指标数据，包括每股收益、每股营业收入、每股经营现金流和每股股东自由现金流，同时返回报告期截止日期和发布时间，用于分析公司的每股盈利能力、收入质量、现金流状况和股东回报水平。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/per-share-indicators --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 财务指标（成长能力）

接口路径：`stock/financial-indicators-growth`
请求方式：**`POST`**
tool_id：`list_stock_fin_ind_growth`

接口说明：通过股票代码（单个或批量）和报告期截止日期范围（格式为yyyy-MM-dd），查询沪深京股票的财务成长能力指标，包括营业收入、净利润、每股收益、总资产、经营性净利润、营业利润、经营活动现金流量净额、净资产等关键财务指标的一年平均增长率以及营业收入和净利润的同比增长率，用于评估公司的成长潜力和财务健康状况。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/financial-indicators-growth --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 财务指标（盈利能力）

接口路径：`stock/financial-indicators-profitab`
请求方式：**`POST`**
tool_id：`list_fin_ind_profit`

接口说明：支持通过单个股票代码、批量股票代码（数组形式），结合报告发布时间范围（格式yyyy-MM-dd）及分页参数，查询A股、B股的盈利能力财务指标数据，返回股票代码、名称、报告发布日期、报告期截止日，以及毛利率、净利率、摊薄ROE、扣非摊薄ROE、ROA、ROIC、总资产报酬率等核心盈利能力指标，可用于股票财务分析、投研评估等场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/financial-indicators-profitab --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 财务指标（营运能力）

接口路径：`stock/financial-indicators-operatin`
请求方式：**`POST`**
tool_id：`list_fin_ind_operating`

接口说明：通过股票代码和日期范围查询沪深京股票的运营能力财务指标，包括固定资产合计周转率、流动资产周转率和应收账款周转率，同时提供股票名称、财报披露日期和报告期截止日期，用于分析企业资产运营效率。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/financial-indicators-operatin --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 财务指标（偿债能力）

接口路径：`stock/financial-indicators-solvency`
请求方式：**`POST`**
tool_id：`list_fin_ind_solvency`

接口说明：通过股票代码（单个或批量）和报告期起止日期（格式为yyyy-MM-dd），查询指定沪深京股票的偿债能力财务指标，包括经营现金流与流动负债比、EBIT利息保障倍数、经营现金流与总负债比、经营现金流与有息债务比、经营现金流与非流动负债比、营业利润与流动负债比、营业利润与总负债比、息税折旧摊销前利润与总负债比、息税折旧摊销前利润与有息债务比等核心数据，用于评估企业的财务风险和偿债能力。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/financial-indicators-solvency --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 财务指标（收现能力）

接口路径：`stock/financial-indicators-cash-col`
请求方式：**`POST`**
tool_id：`list_fin_ind_cash_collect`

接口说明：通过股票代码（单个或批量）和可选的报告期起止日期（格式为yyyy-MM-dd），查询沪深京A股或B股公司的财务现金回收能力指标，包括预收款项营收占比、销售收现营收占比、经营现金流营收占比、经营现金流与资产总额比率、经营现金流与归母净利润比率、经营现金流与营业利润比率等核心数据，同时返回股票信息、报告期截止日期和发布日期（格式为yyyy-MM-dd HH:mm:ss），用于分析企业现金流质量和营收实现效率。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
investoday-api stock/financial-indicators-cash-col --method POST --body-json '{"stockCode":"002594","stockCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---
