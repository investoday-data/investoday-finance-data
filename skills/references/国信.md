# 国信

---

## 沪深京核心实时行情

接口路径：`stock-quote/core`
请求方式：**`POST`**
tool_id：`get_stock_quote_core`

接口说明：通过股票代码查询沪深京市场的核心实时行情数据，返回包括最新价、涨跌幅、成交量、成交额、买卖五档盘口、资金流向、涨跌停状态及行业概念标签等关键指标，所有时间字段统一格式为 yyyy-MM-dd HH:mm:ss，适用于大模型进行即时市场监测、个股异动分析和投资策略辅助。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `codes` | ✅ | array | 股票代码 | `['000001', '600519']` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `data` | 业务数据 | `{}` |

### 接口示例

```bash
investoday-api stock-quote/core --method POST --body-json '{"codes":["000001","600519"]}'
```

---

## 沪深京盘中行情状态

接口路径：`stock-quote/intraday-states`
请求方式：**`POST`**
tool_id：`get_stock_quote_intraday_states`

接口说明：通过传入股票代码列表（可批量），获取沪深京股票的盘中行情状态数据，包含接口快照时间、缓存更新时间、缓存状态等接口相关信息，以及每只股票的交易日期、是否触及涨停、首次触及涨停时间、首次封板时间、封板后是否开板、开板次数、回封次数等核心盘中异动状态数据，可用于实时监测股票盘中表现，辅助交易决策与盘中行情分析。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `codes` | ✅ | array | 股票代码 | `['000001', '600519']` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `data` | 业务数据 | `{}` |

### 接口示例

```bash
investoday-api stock-quote/intraday-states --method POST --body-json '{"codes":["000001","600519"]}'
```

---

## 沪深京实时k线行情

接口路径：`stock-quote/k-line`
请求方式：`GET`
tool_id：`get_stock_quote_k_line`

接口说明：通过股票代码，指定K线周期（支持5分钟、15分钟、30分钟、60分钟、120分钟），可选择设置返回K线数量、复权方式（不复权、前复权、后复权）、分页起始位置，查询沪深京市场股票的实时K线行情数据，包含快照时间、股票基本信息（代码、市场、名称），以及各周期K线的日期时间、开盘价、最高价、最低价、收盘价、成交量、成交额、收益率等核心数据，可用于股票实时技术分析、行情监控等场景。

### 输入参数

**Query 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `code` | ✅ | string | 证券代码 | `000001` |
| `period` | — | string | K 线周期，默认 120min | `120min` |
| `count` | — | integer | 返回最近多少根 K 线，默认 500，最大 700。 最大值:700; | `500` |
| `adjust` | — | string | 复权方式 | `none` |
| `startxh` | — | integer | 分页起点，默认 0。 最小值:0; | `0` |
| `market` | — | string | 可选市场；为空时按代码自动推断 | — |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `data` | 业务数据 | `{}` |

### 接口示例

```bash
# Query 可选参数: period, count, adjust, startxh, market
investoday-api stock-quote/k-line code=000001
```

---

## 市场行情汇总

接口路径：`market/quote-summaries`
请求方式：**`POST`**
tool_id：`get_market_quote_summary`

接口说明：通过申万一级行业代码、申万二级行业代码、概念代码，获取市场行情汇总数据，涵盖整体市场的有效样本数、涨跌家数、涨跌幅、赚钱效应、情绪温度等核心指标，同时提供一级行业、细分行业、概念板块的详细汇总信息，包括成分股涨跌情况、龙头股及领跌股、资金净流入、成交集中度等，可用于市场整体行情分析、行业及概念板块的监测与投研分析

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `industryCodes` | — | array | 申万一级行业代码【与industrySubCodes、conceptCodes组成多选多参数，必须传递其中一个至多个】 | `['410000']` |
| `industrySubCodes` | — | array | 申万二级行业代码【与industryCodes、conceptCodes组成多选多参数，必须传递其中一个至多个】 | `['410100']` |
| `conceptCodes` | — | array | 概念代码【与industryCodes、industrySubCodes组成多选多参数，必须传递其中一个至多个】 | `['CLS80201']` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `data` | 业务数据 | `{}` |

### 接口示例

```bash
# Body JSON 可选参数: industryCodes, industrySubCodes, conceptCodes
investoday-api market/quote-summaries --method POST --body-json '{"industryCodes":["410000"],"industrySubCodes":["410100"],"conceptCodes":["CLS80201"]}'
```

---

## 指数核心实时行情

接口路径：`index-quote/core`
请求方式：**`POST`**
tool_id：`get_index_quote_core`

接口说明：通过传入指数代码数组（可批量）和固定为index的资产类型，获取指数的核心实时行情数据，包括接口快照时间、缓存更新时间等基础信息，以及指数的最新价格、昨收价、开盘价、最高价、最低价、涨跌额、涨跌幅、振幅、成交量、成交额等核心行情指标，还包含近20日涨跌幅、年初至今涨跌幅等数据，可用于实时市场监测、指数走势分析等场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `codes` | ✅ | array | 股票代码 | `['000001', '600519']` |
| `assetType` | ✅ | string | 资产类型（固定为 index） | `index` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `data` | 业务数据 | `{}` |

### 接口示例

```bash
investoday-api index-quote/core --method POST --body-json '{"codes":["000001","600519"],"assetType":"index"}'
```

---

## ETF核心实时行情

接口路径：`fund-quote/core`
请求方式：**`POST`**
tool_id：`get_fund_quote_core`

接口说明：使用ETF代码列表（可批量传入），并指定资产类型为etf，即可获取对应ETF的核心实时行情数据，涵盖证券基本信息（代码、名称、交易所）、关键价格指标（最新价、昨收价、开盘价、最高价、最低价等）、涨跌数据（涨跌额、涨跌幅、振幅等）、成交数据（成交量、成交额、成交均价等）、买卖盘深度（买一到买二、卖一到卖二的报价及量）、ETF专属指标（IOPV、净值、折溢价率等）以及交易日期、行情快照时间等时间信息，适用于ETF实时行情监测、交易决策分析等场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `codes` | ✅ | array | 股票代码 | `['159001', '510300']` |
| `assetType` | ✅ | string | 资产类型（固定为 etf） | `etf` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `data` | 业务数据 | `{}` |

### 接口示例

```bash
investoday-api fund-quote/core --method POST --body-json '{"codes":["159001","510300"],"assetType":"etf"}'
```

---
