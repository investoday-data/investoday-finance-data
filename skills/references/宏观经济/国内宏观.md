# 宏观经济 / 国内宏观

---

## 国债收益率

接口路径：`economic/gover-bond-yield`
请求方式：**`POST`**
tool_id：`list_gover_bond_yield`

接口说明：查询中国国债收益率曲线数据，支持按起止日期（格式为yyyy-MM-dd）筛选，返回指定日期范围内各关键期限（3个月、6个月、2年、10年、30年）的国债收益率，用于宏观经济分析、利率走势研判和固定收益投资决策。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `date` | 发布时间 | `2025-01-01` |
| `bnd3m` | 三月期债券收益率(%) | `1.5475` |
| `bnd6m` | 六月期债券收益率(%) | `1.5475` |
| `bnd2y` | 两年期债券收益率(%) | `1.5475` |
| `bnd10y` | 十年期债券收益率(%) | `1.5475` |
| `bnd30y` | 三十年期债券收益率(%) | `1.5475` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: beginDate, endDate, pageNum, pageSize
investoday-api economic/gover-bond-yield --method POST --body-json '{"beginDate":"2020-01-01","endDate":"2025-01-01","pageNum":1}'
```

---

## 货币供应量

接口路径：`economic/money-supplies`
请求方式：**`POST`**
tool_id：`list_money_supplies`

接口说明：查询中国货币供应量数据，支持按起止日期（格式为yyyy-MM-dd）筛选，返回指定日期范围内各统计日期的M0、M1、M2的绝对数值、同比及环比增长率，以及M1-M2剪刀差，用于宏观经济分析、货币政策研判和流动性监测。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `date` | 截止日期 | `2025-12-30` |
| `m0` | 流通中货币M0(亿元) | `100100` |
| `m1` | 货币M1(亿元) | `100100` |
| `m2` | 货币和准货币M2(亿元) | `100100` |
| `m0YoyPct` | M0同比增长(%) | `20` |
| `m1YoyPct` | M1同比增长(%) | `20` |
| `m2YoyPct` | M2同比增长(%) | `20` |
| `m0QoqPct` | M0环比增长(%) | `20` |
| `m1QoqPct` | M1环比增长(%) | `20` |
| `m2QoqPct` | M2环比增长(%) | `20` |
| `m1M2Spread` | M1-M2剪刀差 | `10` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: beginDate, endDate, pageNum, pageSize
investoday-api economic/money-supplies --method POST --body-json '{"beginDate":"2020-01-01","endDate":"2025-01-01","pageNum":1}'
```

---

## 社融规模存量

接口路径：`economic/social-financing-sto`
请求方式：**`POST`**
tool_id：`list_social_financing_sto`

接口说明：查询中国社会融资规模存量数据，支持按起止日期（格式为yyyy-MM-dd）筛选，返回指定日期范围内各月的社融存量（单位：亿元）及其同比增速，用于宏观经济分析、金融流动性监测和货币政策研判。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `date` | 开始日期 | `2025-12-30` |
| `totalSocialFinStockYoy` | 社融规模存量（亿元） | `10000` |
| `totalSocialFinStockBn` | 社融存量同比增速 | `0.2` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: beginDate, endDate, pageNum, pageSize
investoday-api economic/social-financing-sto --method POST --body-json '{"beginDate":"2020-01-01","endDate":"2025-01-01","pageNum":1}'
```

---

## 货币市场质押式回购指标

接口路径：`economic/money-market-repo-in`
请求方式：**`POST`**
tool_id：`list_money_market_repo_in`

接口说明：查询中国国债收益率曲线数据，支持按起止日期（格式为yyyy-MM-dd）筛选，返回指定日期范围内各关键期限（3个月、6个月、2年、10年、30年）的国债收益率，用于宏观经济分析、利率走势研判和固定收益投资决策。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `date` | 发布日期 | `2025-12-30` |
| `rateType` | 利率品种 | — |
| `openRate` | 开盘利率(%) | `1.3` |
| `closeRate` | 收盘利率(%) | `1.4283` |
| `weightedAvgRate` | 加权利率 | `1.4248` |
| `weightedAvgRateBond` | 加权平均利率（利率债） | `1.4078` |
| `avgRepoTermDays` | 平均回购期限（天） | `1` |
| `dealCount` | 成交笔数（笔） | `18471` |
| `volume` | 成交量（亿元） | `6169.8356` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: beginDate, endDate, pageNum, pageSize
investoday-api economic/money-market-repo-in --method POST --body-json '{"beginDate":"2020-01-01","endDate":"2025-01-01","pageNum":1}'
```

---

## 城乡社会消费品零售总额指标

接口路径：`macro/retail-sales-area`
请求方式：**`POST`**
tool_id：`list_macro_retail_area`

接口说明：支持通过指标代码列表、截止日期范围（格式为yyyy-MM-dd）、页码和页长，查询城乡社会消费品零售总额相关指标数据，涵盖城镇和农村的当期值、累计值、同比增长率、累计增长率等，返回指标中文名称、信息发布日期（格式为yyyy-MM-dd HH:mm:ss）、指标代码、截止日期（格式为yyyy-MM-dd HH:mm:ss）、指标数据，可用于宏观消费市场分析、城乡消费趋势对比等场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indCodes` | — | array | 指标代码列表；为空时返回本接口全部指标 | `[110232455, 110232456]` |
| `beginDate` | — | string | 截止日期起始值，格式yyyy-MM-dd | `2023-01-31` |
| `endDate` | — | string | 截止日期结束值，格式yyyy-MM-dd | `2023-09-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `indName` | 指标中文名称 | `城镇社会消费品零售总额当期值` |
| `pubDt` | 信息发布日期 | `2023-10-18 00:00:00` |
| `indCode` | 指标代码 | `110232455` |
| `endDt` | 截止日期 | `2023-09-30 00:00:00` |
| `dataVal` | 指标数据 | `33984.6` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: indCodes, beginDate, endDate, pageNum, pageSize
investoday-api macro/retail-sales-area --method POST --body-json '{"indCodes":[110232455,110232456],"beginDate":"2023-01-31","endDate":"2023-09-30"}'
```

---

## 人民币汇率查询

接口路径：`macro/exchange-rates`
请求方式：**`POST`**
tool_id：`list_macro_exch_rates`

接口说明：支持通过货币代码、日期范围（开始日期和结束日期格式为yyyy-MM-dd）进行筛选，并可通过页码和页大小控制分页，查询人民币汇率数据，返回货币代码、发布日期（格式yyyy-MM-dd HH:mm:ss）、数据来源、汇率值等核心信息，适用于汇率走势分析、跨境金融业务参考等场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `currencyCode` | — | array | 货币代码 | `["['USD','AUD']"]` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd，对应发布日期） | `2024-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd，对应发布日期） | `2024-12-31` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `currencyCode` | 货币代码 | `USD` |
| `date` | 发布日期 | `2026-08-05 00:00:00` |
| `source` | 来源 | `CFETS` |
| `exchangeRate` | 汇率值 | `7.1234` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: currencyCode, beginDate, endDate, pageNum, pageSize
investoday-api macro/exchange-rates --method POST --body-json '{"currencyCode":["['\''USD'\'','\''AUD'\'']"],"beginDate":"2024-01-01","endDate":"2024-12-31"}'
```

---

## 固定资产投资指标

接口路径：`macro/fixed-asset-investment`
请求方式：**`POST`**
tool_id：`list_macro_fixed_asset`

接口说明：支持通过指定指标代码列表（可批量，为空时返回全部固定资产投资累计增长率指标）、截止日期范围（格式yyyy-MM-dd），并可通过页码和页大小分页，查询固定资产投资相关指标数据，包括指标中文名称、指标代码、信息发布日期（yyyy-MM-dd HH:mm:ss）、截止日期、指标数据值，可用于宏观经济分析、固定资产投资趋势研究等场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indCodes` | — | array | 指标代码列表；为空时返回本接口全部指标 | `[110115280, 110114485]` |
| `beginDate` | — | string | 截止日期起始值，格式yyyy-MM-dd | `2023-01-31` |
| `endDate` | — | string | 截止日期结束值，格式yyyy-MM-dd | `2023-09-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `indName` | 指标中文名称 | `国有及国有控股固定资产投资累计增长率` |
| `pubDt` | 信息发布日期 | `2023-10-18 00:00:00` |
| `indCode` | 指标代码 | `110115280` |
| `endDt` | 截止日期 | `2023-09-30 00:00:00` |
| `dataVal` | 指标数据 | `7.2` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: indCodes, beginDate, endDate, pageNum, pageSize
investoday-api macro/fixed-asset-investment --method POST --body-json '{"indCodes":[110115280,110114485],"beginDate":"2023-01-31","endDate":"2023-09-30"}'
```

---

## 货物贸易指标

接口路径：`macro/goods-trad`
请求方式：**`POST`**
tool_id：`list_macro_goods_trade`

接口说明：货物贸易指标

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indCodes` | — | array | 指标代码列表；为空时返回本接口全部指标 | `[110081729, 110127295]` |
| `beginDate` | — | string | 截止日期起始值，格式yyyy-MM-dd | `2023-01-31` |
| `endDate` | — | string | 截止日期结束值，格式yyyy-MM-dd | `2023-09-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `indName` | 指标中文名称 | `进出口总值当期值` |
| `pubDt` | 信息发布日期 | `2023-10-18 00:00:00` |
| `indCode` | 指标代码 | `110081729` |
| `endDt` | 截止日期 | `2023-09-30 00:00:00` |
| `dataVal` | 指标数据 | `520000` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: indCodes, beginDate, endDate, pageNum, pageSize
investoday-api macro/goods-trad --method POST --body-json '{"indCodes":[110081729,110127295],"beginDate":"2023-01-31","endDate":"2023-09-30"}'
```

---

## 社会融资规模增量指标

接口路径：`macro/social-financing-flow`
请求方式：**`POST`**
tool_id：`list_macro_soc_fin`

接口说明：支持通过指标代码列表（可批量指定，为空时返回全部指标）、统计截止日期范围（格式yyyy-MM-dd），并可配合页码、页大小的分页参数，查询社会融资规模增量相关指标数据，返回指标中文名称、指标代码、信息发布日期、统计截止日期、指标数值，可用于宏观经济形势分析、社会融资市场趋势研究等场景

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indCodes` | — | array | 指标代码列表；为空时返回本接口全部指标 | `[150000075, 150000076]` |
| `beginDate` | — | string | 截止日期起始值，格式yyyy-MM-dd | `2023-01-31` |
| `endDate` | — | string | 截止日期结束值，格式yyyy-MM-dd | `2023-09-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `indName` | 指标中文名称 | `社会融资规模增量` |
| `pubDt` | 信息发布日期 | `2023-10-18 00:00:00` |
| `indCode` | 指标代码 | `150000075` |
| `endDt` | 截止日期 | `2023-09-30 00:00:00` |
| `dataVal` | 指标数据 | `41200` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: indCodes, beginDate, endDate, pageNum, pageSize
investoday-api macro/social-financing-flow --method POST --body-json '{"indCodes":[150000075,150000076],"beginDate":"2023-01-31","endDate":"2023-09-30"}'
```

---

## 财政支出指标

接口路径：`macro/fiscal-expenditur`
请求方式：**`POST`**
tool_id：`list_macro_fiscal_exp`

接口说明：支持通过指标代码列表、截止日期范围（格式yyyy-MM-dd）、页码和页大小，查询国家财政支出累计值、国家财政支出累计增长率等财政支出指标数据，返回指标中文名称、信息发布日期（格式yyyy-MM-dd HH:mm:ss）、指标代码、截止日期（格式yyyy-MM-dd HH:mm:ss）、指标具体数值，可用于宏观经济分析、财政政策研究等场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indCodes` | — | array | 指标代码列表；为空时返回本接口全部指标 | `[110002480, 110002481]` |
| `beginDate` | — | string | 截止日期起始值，格式yyyy-MM-dd | `2023-01-31` |
| `endDate` | — | string | 截止日期结束值，格式yyyy-MM-dd | `2023-09-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `indName` | 指标中文名称 | `国家财政支出累计值` |
| `pubDt` | 信息发布日期 | `2023-10-18 00:00:00` |
| `indCode` | 指标代码 | `110002480` |
| `endDt` | 截止日期 | `2023-09-30 00:00:00` |
| `dataVal` | 指标数据 | `191000` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: indCodes, beginDate, endDate, pageNum, pageSize
investoday-api macro/fiscal-expenditur --method POST --body-json '{"indCodes":[110002480,110002481],"beginDate":"2023-01-31","endDate":"2023-09-30"}'
```

---

## 房地产投资指标

接口路径：`macro/real-estate-inves`
请求方式：**`POST`**
tool_id：`list_macro_re_invest`

接口说明：支持通过指标代码列表（可指定具体房地产投资相关指标，如房地产投资累计值、住宅投资累计增长率、办公楼投资累计值等）、时间范围（开始日期、结束日期，格式为yyyy-MM-dd）以及分页参数，查询房地产投资相关指标数据，返回指标中文名称、信息发布日期、指标代码、截止日期、指标数据，可用于房地产行业投资情况的统计分析与趋势研究。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indCodes` | — | array | 指标代码列表；为空时返回本接口全部指标 | `[110116170, 110116171]` |
| `beginDate` | — | string | 截止日期起始值，格式yyyy-MM-dd | `2023-01-31` |
| `endDate` | — | string | 截止日期结束值，格式yyyy-MM-dd | `2023-09-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `indName` | 指标中文名称 | `房地产投资累计值` |
| `pubDt` | 信息发布日期 | `2023-10-18 00:00:00` |
| `indCode` | 指标代码 | `110116170` |
| `endDt` | 截止日期 | `2023-09-30 00:00:00` |
| `dataVal` | 指标数据 | `87269` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: indCodes, beginDate, endDate, pageNum, pageSize
investoday-api macro/real-estate-inves --method POST --body-json '{"indCodes":[110116170,110116171],"beginDate":"2023-01-31","endDate":"2023-09-30"}'
```

---

## 人民币信贷收支指标

接口路径：`macro/rmb-credi`
请求方式：**`POST`**
tool_id：`list_macro_rmb_credit`

接口说明：人民币信贷收支指标

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indCodes` | — | array | 指标代码列表；为空时返回本接口全部指标 | `[191000657, 191000660]` |
| `beginDate` | — | string | 截止日期起始值，格式yyyy-MM-dd | `2023-01-31` |
| `endDate` | — | string | 截止日期结束值，格式yyyy-MM-dd | `2023-09-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `indName` | 指标中文名称 | `各项存款` |
| `pubDt` | 信息发布日期 | `2023-10-18 00:00:00` |
| `indCode` | 指标代码 | `191000657` |
| `endDt` | 截止日期 | `2023-09-30 00:00:00` |
| `dataVal` | 指标数据 | `2890000` |
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |

### 接口示例

```bash
# Body JSON 可选参数: indCodes, beginDate, endDate, pageNum, pageSize
investoday-api macro/rmb-credi --method POST --body-json '{"indCodes":[191000657,191000660],"beginDate":"2023-01-31","endDate":"2023-09-30"}'
```

---
