# 工具 / 图标

> 本文件由 `create/generate_references.py` 自动生成，请勿手动修改。

---

####  echarts转图片Base64(标准)

接口：`generate_chart`

将ECharts配置选项转换为Base64编码的图片，用于将动态图表数据快速生成为静态图片以便嵌入报告或分享。

**输入参数**

_无参数_

**接口示例**

```bash
python skills/scripts/call_api.py generate-chart
```

---

#### echarts转图片Base64(组件)

接口：`generate_nex_chart`

根据提供的ECharts图表配置选项（options）和唯一标识符（id），生成对应的图表图片，并以Base64编码的字符串形式返回，便于大模型在需要可视化数据时快速生成和嵌入图表。

**输入参数**

_无参数_

**接口示例**

```bash
python skills/scripts/call_api.py generate-nex-chart
```

---
