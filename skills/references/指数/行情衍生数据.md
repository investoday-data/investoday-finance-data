# 指数 / 行情衍生数据

---

## 指数区间涨幅

接口路径：`index/range-gains`
请求方式：`GET`
tool_id：`get_index_range_gains`

接口说明：通过指数代码查询指定指数的多时间区间涨跌幅数据，返回包括指数代码、名称以及近一日、近一周、近一月、近三月、近半年、近一年和今年以来等多个维度的涨跌幅百分比，用于分析指数的短期、中期和长期表现趋势。

### 输入参数

**Query 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indexCode` | ✅ | string | 指数代码 | `000300` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `indexCode` | 指数代码 | `000001` |
| `indexName` | 指数名称 | `平安银行` |
| `return1dPct` | 近一日涨跌幅(%) | `-3.75` |
| `return1wPct` | 近一周涨跌幅(%) | `-3.75` |
| `return1mPct` | 近一月涨跌幅(%) | `-3.75` |
| `return3mPct` | 近三月涨跌幅(%) | `-15.73` |
| `return6mPct` | 近半年涨跌幅(%) | `-15.73` |
| `return1yPct` | 近一年涨跌幅(%) | `-12.75` |
| `returnYtdPct` | 今年以来涨跌幅(%) | `-12.75` |

### 接口示例

```bash
investoday-api index/range-gains indexCode=000300
```

---

## 指数量价与压力支撑指标

接口路径：`index/price-vol-ind`
请求方式：**`POST`**
tool_id：`list_index_price_volume_indicators`

接口说明：支持通过单个指数代码或批量指数代码，结合指定时间范围（开始日期、结束日期格式均为yyyy-MM-dd），分页获取指数的量价与压力支撑类技术指标数据，包括指数代码、指数名称、交易日期，以及布林带、多空布林线、ENV、MIKE、MFI、OBV、PVT、WVAD、AR、BR、PSY、CR等各类技术指标数值，可用于指数的技术面分析、压力支撑位判断等投研场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indexCode` | — | string | 指数代码【与indexCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `indexCodes` | — | array | 指数代码列表【与indexCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: indexCode, indexCodes, beginDate, endDate, pageNum, pageSize
investoday-api index/price-vol-ind --method POST --body-json '{"indexCode":"002594","indexCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 指数摆动与超买超卖指标

接口路径：`index/osc-indicators`
请求方式：**`POST`**
tool_id：`list_index_oscillator_indicators`

接口说明：支持通过单个指数代码或指数代码列表，结合指定时间范围（开始日期、结束日期格式均为yyyy-MM-dd），查询指数的摆动与超买超卖类技术指标数据，返回交易日期（格式为yyyy-MM-dd HH:mm:ss）、指数代码、指数名称，以及RC变化率、ATR真实波幅、CVLT蔡金离散指标、ADTM动态买卖气指标、KDJ指标（K/D/J线）、5日及14日RSI、乖离率、6日及24日乖离率等核心技术指标，同时支持分页查询，可用于指数的趋势分析、超买超卖判断等技术分析场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indexCode` | — | string | 指数代码【与indexCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `indexCodes` | — | array | 指数代码列表【与indexCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: indexCode, indexCodes, beginDate, endDate, pageNum, pageSize
investoday-api index/osc-indicators --method POST --body-json '{"indexCode":"002594","indexCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---

## 指数强弱与趋向指标

接口路径：`index/str-trend-ind`
请求方式：**`POST`**
tool_id：`list_index_strength_trend_indicators`

接口说明：支持通过单个指数代码或批量指数代码数组，结合指定的时间范围（开始日期、结束日期格式为yyyy-MM-dd），并可通过页码、页长分页，查询指数的强弱与趋势技术指标数据，包括交易日期（格式为yyyy-MM-dd HH:mm:ss）、指数代码及名称，以及5日、10日、20日、60日、120日、250日移动平均线，MACD、DMI、DMA、MTM、TRIX等多类技术指标数值，可用于指数趋势判断、强弱分析等投资研究场景。

### 输入参数

**Query 参数**

_无参数_

**Body JSON 参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `indexCode` | — | string | 指数代码【与indexCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `indexCodes` | — | array | 指数代码列表【与indexCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

### 输出参数

| 字段名 | 说明 | 示例 |
|--------|------|------|
| `pageNum` | page number | `1` |
| `pageSize` | page size | `100` |
| `totalCount` | total count | `0` |
| `data` |  | — |

### 接口示例

```bash
# Body JSON 可选参数: indexCode, indexCodes, beginDate, endDate, pageNum, pageSize
investoday-api index/str-trend-ind --method POST --body-json '{"indexCode":"002594","indexCodes":["000001","600519"],"beginDate":"2020-01-01"}'
```

---
